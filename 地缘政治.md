# 地缘政治实时信息获取来源和处理方式

## 结论

当前项目里“地缘政治实时信息”的唯一实时外部来源是 `GDELT DOC API`，实现代码在 `python_engine/collector/gdelt_collector.py`。系统没有接入专门的商业地缘政治情报源，也没有单独抓取网站页面；它是通过 GDELT 的新闻事件检索结果，提取语气分布和文章数量，再转换成若干地缘政治风险因子。

需要明确的一点是：

- 当前实时采集器实际更新的是 6 个 GDELT 相关因子。
- 历史数据集 `OilRisk_System/Final_Oil_Dataset_Cleaned.csv` 中存在更多更细的地缘政治字段，但这些细分字段并没有被当前实时采集链路全部刷新。
- 当前实时推理直接使用的核心地缘相关输入，主要是“新闻情感指数”和“地缘政治事件综合评分”。

## 1. 实时信息来源

### 1.1 数据源

实时来源为：

- `https://api.gdeltproject.org/api/v2/doc/doc`

对应实现：

- `python_engine/collector/gdelt_collector.py`

代码中明确写死了 `GDELT_DOC_API = "https://api.gdeltproject.org/api/v2/doc/doc"`，并通过共享 HTTP Session 发起请求。

### 1.2 是否需要 Key

当前 GDELT 采集器不需要 API Key。

代码里 `GdeltCollector` 的说明就是 `GDELT data collector (no API key required)`，这和 FRED/EIA 不同。

### 1.3 采集的实时主题

当前实时采集定义了 5 个地缘政治/风险主题查询，加 1 个新闻情感查询：

1. 地缘政治综合评分
   - 查询词：`(oil OR crude OR petroleum OR energy) AND (conflict OR war OR sanction OR crisis)`
2. 俄乌冲突
   - 查询词：`(Russia OR Ukraine OR Russian OR Ukrainian) AND (oil OR gas OR energy OR pipeline)`
3. 中东局势
   - 查询词：`(Iran OR Saudi OR Yemen OR Iraq OR Syria) AND (oil OR energy OR conflict)`
4. 红海航运风险
   - 查询词：`(Red Sea OR Houthi OR Suez) AND (shipping OR oil OR tanker)`
5. GPR 近似指数
   - 查询词：`geopolitical risk AND (oil OR energy OR commodity)`
6. 能源市场新闻情感
   - 查询词：`"energy market" AND (oil OR crude OR petroleum)`

这些查询都围绕“原油/能源市场相关的地缘政治新闻”构造，不是泛政治新闻抓取。

## 2. 实时处理方式

## 2.1 时间窗口

每次采集不是只看当天零点到 24 点，而是围绕目标日期构造一个前后各 1 天的窗口：

- `startdatetime = target_date - 1 day`
- `enddatetime = target_date + 1 day`

格式为 `YYYYMMDDHHMMSS`。

这意味着系统拿到的是“围绕目标交易日的一段新闻密度和语气分布”，而不是严格自然日闭区间。

## 2.2 两类 GDELT 请求模式

系统对每个地缘主题会发两类请求：

1. `mode=tonechart`
   - 用来取语气分布时间线
   - 核心用途：计算平均语气，再映射风险分数
2. `mode=artlist`
   - 用来取文章列表
   - 核心用途：用文章数量作为事件强度代理

也就是说，当前处理逻辑不是做正文 NLP，而是：

- 用 `tonechart` 代表新闻语气
- 用 `artlist` 的文章数代表事件热度

## 2.3 语气数据处理

`tonechart` 返回的数据会先做结构归一化：

- 如果返回是 list，直接筛出 dict 项
- 如果返回是 dict，则优先取 `timeline` 或 `tonechart`

之后系统读取每个桶里的：

- `bin`：语气值
- `count`：该语气桶对应的文章数

再按加权平均计算 `avg_tone`：

`avg_tone = sum(bin * count) / sum(count)`

## 2.4 地缘政治综合评分计算方式

当前实现把 GDELT 语气和文章量映射成 `0-10` 风险分数，逻辑如下：

1. 先根据平均语气得到基础分：
   - `score = clamp(5.0 - avg_tone * 0.5, 0, 10)`
2. 再根据文章量得到强度系数：
   - `volume_factor = clamp(volume / 50.0, 0.5, 2.0)`
3. 最终输出：
   - `final = round(score * volume_factor / 2.0, 2)`

可见当前设计的含义是：

- 新闻语气越负面，风险分越高
- 相关新闻越多，风险会被放大
- 但放大量有上下限，不会无限增大

## 2.5 新闻情感指数计算方式

新闻情感指数不是 `0-10`，而是映射到 `[-1, 1]`：

1. 从 `tonechart` 提取全部 `bin`
2. 直接算平均值 `avg`
3. 映射为：
   - `sentiment = clamp(avg / 10.0, -1.0, 1.0)`

所以：

- 越接近 `-1`，代表能源相关新闻越负面
- 越接近 `1`，代表能源相关新闻越正面

## 2.6 容错和退化策略

当前实现做了这些容错处理：

- GDELT 请求间最少间隔 `2.0` 秒，避免免费 API 触发 `429`
- 单次请求超时 `30` 秒
- 如果返回 `Content-Type` 不是 JSON，直接判失败
- 如果响应体为空，直接判失败
- 如果 JSON 解析失败，记录 warning 并返回空
- 如果主题查询失败：
  - 地缘评分默认退化到 `5.0`
  - 新闻情感默认退化到 `0.0`

这说明系统优先保证“链路不断”，即使 GDELT 异常，也会给出中性默认值，而不是让整个采集任务崩掉。

## 2.7 网络层处理

所有采集器共享 `python_engine/collector/http_utils.py` 提供的 Session，具备：

- 代理支持：读取 `HTTP_PROXY` / `HTTPS_PROXY`
- SSL 验证开关：`SSL_VERIFY=false` 时可跳过校验
- 重试机制：对 `429/500/502/503/504` 和连接问题做重试
- 连接池复用

这部分设计主要是为了适应中国网络环境，降低 GDELT/FRED/EIA 的连接失败率。

## 3. 系统内的落库方式

## 3.1 因子注册

GDELT 实时采集器当前注册了 6 个因子：

1. `GEOPOLITICS_SCORE`
   - 落库列名：`Risk_Geopolitics_地缘政治事件综合评分`
2. `NEWS_SENTIMENT`
   - 落库列名：`Risk_Sentiment_新闻情感指数__News_Sentiment`
3. `GEO_RUSSIA_UKRAINE`
   - 落库列名：`Risk_Geopolitics_俄乌冲突`
4. `GEO_MIDDLE_EAST`
   - 落库列名：`Risk_Geopolitics_中东局势_伊朗_沙特_也门`
5. `GEO_RED_SEA`
   - 落库列名：`Risk_Geopolitics_红海航运危机_也门胡塞武装`
6. `GPR_INDEX`
   - 落库列名：`Risk_Geopolitics_情绪组_GPR__Value`

这些元数据定义在 `python_engine/collector/registry.py`。

## 3.2 写入的数据库表

采集结果先写入 `factor_realtime` 表，字段包括：

- `date`
- `factor_key`
- `column_name`
- `value`
- `source`
- `frequency`

其中 GDELT 因子的：

- `source = gdelt`
- `frequency = daily`

写入方式是按 `(date, factor_key)` 做 upsert。

## 3.3 采集日志和状态

每次采集后还会写两类状态数据：

1. `collection_log`
   - 记录来源、成功/失败数量、错误详情、耗时
2. `system_state`
   - 更新 `last_collection_time`
   - 更新 `last_collection_status`
   - 更新 `data_date`

所以系统不仅保存结果，也保留了采集任务的运行痕迹。

## 4. 调度方式

GDELT 采集任务由 `python_engine/collector/scheduler.py` 调度：

- 任务名：`collect_gdelt`
- 调度规则：`mon-fri` 每天 `12:00`

这意味着它是工作日中午执行一次，不是分钟级或小时级实时流式拉取。

严格来说，这套系统的“实时”更接近：

- 每日定时更新的近实时因子

而不是：

- 秒级实时事件监控

## 5. 与模型的关系

## 5.1 当前实时推理直接使用的地缘相关字段

在 `python_engine/engine.py` 和 `OilRisk_System/OilRisk_Engine.py` 中，当前 `MARKET_STATE_COLS` 直接纳入 Stacking 输入的地缘/情绪字段是：

- `Risk_Sentiment_新闻情感指数__News_Sentiment`
- `Risk_Geopolitics_地缘政治事件综合评分`

也就是说，当前实时推理最直接依赖的是：

- 新闻情感指数
- 地缘政治综合评分

## 5.2 已采集但未在当前核心输入中直接出现的字段

以下 GDELT 因子虽然会实时采集并写库，但没有出现在当前 `MARKET_STATE_COLS` 中：

- `Risk_Geopolitics_俄乌冲突`
- `Risk_Geopolitics_中东局势_伊朗_沙特_也门`
- `Risk_Geopolitics_红海航运危机_也门胡塞武装`
- `Risk_Geopolitics_情绪组_GPR__Value`

它们可能用于：

- 后续扩展
- 解释展示
- 历史分析对照

但从当前推理主路径看，它们不是 Stacking 的直接输入。

## 5.3 历史数据集与当前实时链路的差异

`OilRisk_System/Final_Oil_Dataset_Cleaned.csv` 中存在更多地缘政治字段，例如：

- 利比亚油田冲突
- 美国对委内瑞拉制裁
- OPEC+ 会议结果影响
- 国际能源政策与能源制裁
- 霍尔木兹海峡安全风险
- 巴以冲突外溢风险
- 伊朗核问题相关制裁与谈判
- 地缘事件持续周期

但当前 `gdelt_collector.py` 并没有实时生成这些字段。

因此应当明确：

- 历史训练数据中的地缘字段更丰富
- 当前在线实时采集链路只覆盖其中一部分
- 线上实时因子覆盖范围小于历史特征全集

## 6. 处理链路总结

当前项目中，地缘政治实时信息的完整链路是：

1. 调度器在工作日 12:00 触发 `collect_gdelt`
2. `GdeltCollector` 对预定义主题调用 GDELT DOC API
3. 用 `tonechart` 提取语气分布，用 `artlist` 统计文章量
4. 把语气和文章量映射成风险分和情感值
5. 将结果 upsert 到 `factor_realtime`
6. 记录 `collection_log` 和 `system_state`
7. 当系统执行全量采集或其他触发推理的流程时，数据库中的最新因子会被 `engine.reload_from_db()` 合并到历史数据，再参与预测

## 7. 当前实现的边界和问题

### 7.1 优点

- 实现简单，依赖少，不需要额外商业 API Key
- 与原油/能源主题强绑定，不是泛新闻抓取
- 有统一代理、重试、限频和默认值兜底
- 能沉淀为结构化因子，而不只是原始新闻文本

### 7.2 局限

- 本质上是“基于新闻检索结果的代理变量”，不是事件级知识图谱
- 只有工作日每日一次调度，不是高频实时监控
- 查询词是手工写死的，覆盖面受限
- 风险评分公式是经验映射，不是经过严格标定的统计模型
- 历史数据中的很多细分地缘字段，当前并没有实时更新

### 7.3 一个重要实现细节

`collect_gdelt()` 当前调用 `_run_collection(..., trigger_inference=False)`。

这意味着：

- 单独执行 GDELT 采集任务时，只会写入实时因子，不会立即触发预测
- 只有全量采集或其他启用推理的链路，才会把这些新因子立刻带入预测结果

这点很关键，因为它决定了“地缘政治信息更新了”并不等于“前端风险指数立刻更新了”。

## 8. 一句话概括

这个项目的地缘政治实时信息获取，本质上是：用 GDELT 的能源相关新闻检索结果，按预设主题提取语气和热度，映射成每日更新的地缘政治风险因子，再写入数据库供后续风控模型使用。
