**👨‍💻 给后端同学（API 封装任务）：**

1. **环境准备：** 解压后，请在你的环境里运行 pip install torch xgboost pandas numpy scikit-learn joblib。
2. **测试运行：** 直接运行 python OilRisk\_Engine.py，你就能看到终端打印出最终的 JSON 数据。
3. **你的核心开发任务：** \* 请用 FastAPI（或 Flask / SpringBoot）把 OilRisk\_Engine.py 包装成一个 API 接口。
    * 现在的代码里，我在最下面写了 engine.predict\_today(df)。你只需要写一个路由（比如 /api/get\_risk），在里面调用这个方法，然后把返回的 JSON 直接抛给前端即可。

**🎨 给前端同学（大屏对接任务）：** 后端马上会给你提供真正的接口，返回的 JSON 结构如下，请提前把页面上的假数据替换成对应的字段映射：

* **仪表盘分数：** 读 dashboard.score（0-100分，目前是 0 分）
* **风险红绿灯：** 读 dashboard.level（"高风险🔴" / "中风险🟡" / "低风险🟢"）
* **LSTM 预测涨幅：** 读 expert\_signals.lstm\_impl\_return
* **XGBoost 极端风险：** 读 expert\_signals.xgb\_risk\_score
* **右侧 Top5 归因柱状图：** 遍历读取 top\_factors 数组里的 name 和 value。

目前接口已经 100% ready，随时可以开始联调！数据这块我们目前先读取静态的 CSV（Final\_Oil\_Dataset\_Cleaned.csv），等联调跑通大屏展示后，我们再看需不需要加自动更新脚本。

