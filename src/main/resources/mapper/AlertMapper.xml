<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.oilrisk_alert.mapper.AlertMapper">

    <select id="findByDateRange" resultType="com.example.oilrisk_alert.entity.Alert">
        SELECT id, date, level, risk_index, trigger_type, trigger_factor,
               trigger_factor_zh, summary, summary_en, detail, ai_report, created_at
        FROM alert
        WHERE date BETWEEN #{param1} AND #{param2}
        ORDER BY date ASC
    </select>

    <select id="countByLevel" resultType="long">
        SELECT COUNT(*)
        FROM alert
        <where>
            <if test="level != null and level != ''">
                level = #{level}
            </if>
        </where>
    </select>

    <select id="findPage" resultType="com.example.oilrisk_alert.entity.Alert">
        SELECT id, date, level, risk_index, trigger_type, trigger_factor,
               trigger_factor_zh, summary, summary_en, detail, ai_report, created_at
        FROM alert
        <where>
            <if test="level != null and level != ''">
                level = #{level}
            </if>
        </where>
        <choose>
            <when test="sort == 'riskIndex' or sort == 'risk_index'">
                ORDER BY risk_index
            </when>
            <otherwise>
                ORDER BY date
            </otherwise>
        </choose>
        <choose>
            <when test="order == 'asc'">
                ASC
            </when>
            <otherwise>
                DESC
            </otherwise>
        </choose>
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="findById" resultType="com.example.oilrisk_alert.entity.Alert">
        SELECT id, date, level, risk_index, trigger_type, trigger_factor,
               trigger_factor_zh, summary, summary_en, detail, ai_report, created_at
        FROM alert
        WHERE id = #{param1}
    </select>

    <update id="updateAiReport">
        UPDATE alert SET ai_report = #{aiReport} WHERE id = #{id}
    </update>

</mapper>
