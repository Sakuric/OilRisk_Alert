<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.oilrisk_alert.mapper.RiskMapper">

    <select id="findLatest" resultType="com.example.oilrisk_alert.entity.RiskIndex">
        SELECT id, date, risk_index, risk_level, oil_price, created_at
        FROM risk_index
        ORDER BY date DESC
        LIMIT 1
    </select>

    <select id="findByDateRange" resultType="com.example.oilrisk_alert.entity.RiskIndex">
        SELECT id, date, risk_index, risk_level, oil_price, created_at
        FROM risk_index
        WHERE date BETWEEN #{param1} AND #{param2}
        ORDER BY date ASC
    </select>

</mapper>
